import { Placeholder } from "@/components/Placeholder";

export default function BoardPage() {
  return (
    <Placeholder
      title="匿名掲示板"
      branch="feat/board"
      note="表示名は自動生成しますが、内部の authorId は保持します。画面には絶対に出さないこと。"
    />
  );
}
