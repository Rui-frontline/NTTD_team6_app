@AGENTS.md

.envまたは.env.localの内容を読まないでください。

## README.md の更新

`README.md` は仕様書ではなく、**「いま何がどこで動いているか」の索引**です。実装と食い違うと、読んだ人が存在しない前提で作り始めてしまいます。

以下をやったときは、**同じPRの中で** `README.md` も直してください。あとで直す前提にすると、まず残りません。

- **画面を追加した / URLを変えた** → 「画面と、それを動かしているファイル」の表に足す
- **機能を追加した** → 「機能ごとの実装場所」に節を足し、**入口になるファイル**を書く
- **`supabase/` に `.sql` を足した** → SQLの表に足す。**「流さないと何が起きるか」まで書く**（これが無いと、実行し忘れた人が原因に辿り着けません）
- **環境変数を増やした** → 「環境変数」に足す
- **`lib/repository.ts` に関数を足した** → 関連する機能の節から辿れるようにする

### 書き方

**何をしたかではなく、次に触る人が知りたいことを書いてください。**

- 機能の説明は数行で足ります。詳しい理由はコード中のコメントとコミットメッセージに書き、READMEからはファイルへのリンクで繋ぎます
- **同じ値が2箇所以上にある場合は必ず書いてください。** 片方だけ直すと壊れる箇所は、読まないと気づけません（例: 口コミのしきい値が `lib/reviews.ts` と `supabase/reviews.sql` の両方にある）
- **実際に時間を溶かした落とし穴は残してください。** 「存在しないCSS変数を使うと `border-color` が `currentColor` になる」のような、読んだだけでは分からないものです
- ファイルへのリンクは相対パスで書き、**実在することを確認してください**

### 消すとき

機能を削除したりファイルを移したときは、**READMEの該当箇所も消してください。** 残った記述は、間違った案内として動き続けます。
